# PRODUCT.md · QuizMaster

> 由 impeccable init 建立。记录产品事实，不记录视觉决策（视觉世界见 DESIGN.md）。

## 是什么

一个**在线知识竞答系统**，题材为**南开大学校史**。对应综合实践课程第 17 题。

学生登录后按分类/难度随机抽题作答，成绩、评级、统计与排行**全部由服务端决定**；
在此闭环之上叠加两个可一键关闭的增强：**研习积分与成就体系**、**基于课程讲义的轻量 RAG 解析**。

## 谁在用

| 角色 | 场景 | 关心什么 |
|---|---|---|
| STUDENT（主用户） | 课后自测、课程演示、现场答题 | 题好不好答、分数怎么来的、能不能回看解析 |
| ADMIN | 题库维护、讲义导入、奖励重算 | 改题是否影响历史成绩、数据能否复现 |
| 验收者 / 教师 | 现场投屏演示、检查是否可运行 | 3 条命令能否跑起来、口径能否当场验算 |
| 截图/录屏 | 演示视频素材 | 一屏能读完、不滚动也能看清 |

真正的使用场景是**课堂演示与作业验收**：同一台笔记本、投影仪、固定的 1280×720 视口，
需要在一两分钟内讲清「闭环 + 服务端权威 + 两个增强」，不能卡顿、不能有内容看不见。

## 成功标准

1. **可运行**：`uv sync` → `init_db.py --reset` → `run.py`，三条命令起来，数据可持久化。
   **没装 uv 也一样**：`python -m venv .venv` + `pip install -r requirements.txt` 是等价的三条命令，
   两组命令装出的依赖版本一致（见 `README.md` §7）。
2. **可验证**：判分口径、SP 公式、成就规则都能当场纸笔验算，且有自动化测试兜底（104 项单测 + 87 项冒烟）。
3. **可降级**：AI 不可用、JS 失效、用户开启「减少动态效果」时，核心流程依然完整。
4. **可讲解**：页面自己要把「规则由服务端决定」讲明白，而不是靠讲解者补话。

## 非目标

- 不做实时多人对战、房间匹配、积分商城、题型扩展中的多选与主观题（单选 / 判断 / 填空题已支持）。
- 不做微服务、消息队列、向量数据库。
- 不做移动端原生应用；响应式只覆盖到浏览器窗口收窄。
- **不做用户上传内容、不做任何社交功能。**

## 内容事实（不可改动）

- 题库 80 题，四个分类：创校与发展 / 人物与校友 / 校园与文化 / 精神与学科。
- 题型分布：单选 40 / 判断 20 / 填空 20。
- 难度分布：EASY 16 / MEDIUM 39 / HARD 25。
- 计分口径 A-1：`得分 = round(正确率 × 100)`，未答按错计入分母，用时不影响得分。
- 判分按题型分派：单选与判断按键位比对；填空题按「命中任一关键字 + 无污言秽语 + 非离谱输入」判对（宽松口径，见 `docs/DESIGN.md` §9.1）。
- SP 公式：`20 + min(round(得分×0.5), 50) + 难度加成(0/5/10) + min(5×(连续天数−1), 25)`，单局上限 105。
- 5 级称号：研习者 / 学者 / 研究员 / 资深研究员 / 首席研习官。
- 8 项成就，均由终态会话派生、可重算复现。
- 每题时限 60 秒，服务端倒计时；`TEST_TIME_LIMIT_SECONDS` 仅 development 生效。

## 技术约束（决定了能做什么视觉）

- **服务端渲染为主**（Jinja2），页面在 JS 失效时必须完整可读。任何动效只能是增强。
- 前端**无构建步骤**：原生 HTML/CSS/JS，不引入打包器、不引 CDN 依赖。
- 单机 SQLite（WAL），零外部服务依赖。
- 页面生命周期短、无 SPA 路由；交互集中在答题页、结果页、知识库三处。
- 演示机是普通笔记本，动效必须便宜（只动 `transform` / `opacity`）。

## 品牌与语调

- 题材是大学校史，语调应当**庄重但不沉重、有书卷气但不古板**。
- 校训「允公允能，日新月异」是内容资产，可以引用，不可改写。
- 界面文案用中文，用户是同学与老师，不用营销腔。
