# .impeccable.md · QuizMaster 设计上下文

> 本文件供前端设计类 skill（impeccable）读取，约束所有视觉改动。
> 与 `docs/DESIGN.md` 第 5 章「视觉设计」互为表里：本文写**为什么**，DESIGN.md 写**是什么**。
>
> **时效说明（重要）**：本文最初写于**第一版（深色实景）**时期。第一版的视觉世界已在
> **第二轮整体重设计**中被整体替换（见 `docs/DESIGN.md` §5.4），第五轮又对知识库做了板块重构
> （§5.7）。下面「Brand Personality」「Design Principles」两条**依然有效且必须遵守**；
> 「Aesthetic Direction」与「已知审计结论」已按现状改写 / 标注，**不要再照第一版的深色方案改**。

## Design Context

### Users

- **主要使用者：同学（首次接触南开的在校生 / 参赛者）**。场景是课间或宿舍用笔记本打开，一局 10 题、每题 60 秒。他们的目标不是"欣赏界面"，而是**快速开考、答得顺、看得懂反馈**。任何需要停下来琢磨的元素都是负担。
- **次要使用者：教师 / 助教（现场验收与投屏演示）**。场景是投影仪 + 一台笔记本，在 1280×720 或 1440×900 下被围观。他们需要**一屏讲清**：分数怎么来的、知识库讲了什么、题目出处是哪。所以知识库要有"放映模式"，结果页要能现场纸笔验算。
- **管理员（题库与资料库维护）**。低频、目标明确：找到那道题、改掉、别影响已有成绩。

### Brand Personality

**庄重 · 有据 · 可亲近**。

- 庄重：这是校史题材，气质应对得起"南开"两个字——克制的深色、衬线标题、金色作为荣誉色，不喧哗。
- 有据：每一处陈述都要能指到出处（题卡背面有来源、结果页有得分拆解、档案有口径说明）。**"可被追问"本身就是一种视觉气质**：排版干净、层级清楚、数字突出。
- 可亲近：面向学生，不能变成纪念馆。翻卡、印章、时间轴这些"可以上手玩"的形式，是让校史可读的手段，不是装饰。

情绪目标：打开时**安静而正式**，答题时**专注不慌**，出分时**被认真对待**。

### Aesthetic Direction

> **以下是第二版（当前生效）方向**；第一版「深色实景杂志风」已在第二轮整体重设计中被替换，不要再沿用。

- **视觉方向**：手绘水彩 + 纸感编辑设计，明亮、克制、信息密度适中。不是 SaaS 后台，也不是游戏化霓虹。
- **主题**：只用浅色。米白纸面 `#faf5ea` 打底、暖白卡片 `#fffdf8`、墨色文字 `#2c3742`；
  主色水彩青绿 `#34756a`，强调色赭石 `#b8763c`，荣誉 / 印章用银杏金 `#c99a3f`。
  不做深色模式，避免风格分裂。
- **保留的既有资产**：6 张自有手绘水彩插画（首页横幅 4 帧 1152×512、登录页与结果页各 1 张 960×594）、
  楷体承担大字号装饰、8 个同源内联 SVG 成就图标、「NK 书与银杏」品牌徽记。
- **参考锚点**：纸质展陈册的图文关系 —— 插画放在有边界的槽位里完整可见，文字落在亮纸面上，
  信息按「年代 / 人物 / 物件」分组。
- **反例（明确不要）**：
  - 紫蓝渐变 + 发光边框 + 玻璃拟态堆叠（典型 AI 味 SaaS）。
  - 万物皆卡片、卡片套卡片。
  - 圆角矩形 + 通用投影当作唯一的层次手段。
  - 用 emoji 当图标；用不同字重的 Unicode 符号凑一套导航图标。
  - 深色遮罩压白字盖住图片；**用了大图却看不清，是最贵的浪费**。
  - 白字直接压在插画或渐变图上 —— 多帧明度差极大，总有一帧读不清；图注一律落在「纸标签」上。

### Design Principles

1. **实景要真的看得见。** 用照片就必须让建筑可辨认、光影有方向；遮罩只负责把文字压到位（对比度达标），不负责把画面抹平。宁可缩小用图面积、把照片放在有边界的容器里，也不要全屏拉伸一张被压暗的图。
2. **动静服务于状态。** 只动 `transform` / `opacity`，只做"状态变化"和"阅读引导"，不做无意义的飘屏；`prefers-reduced-motion` 下全部退化为即时状态。动效永远不能成为内容可读的前提。
3. **数字与结论先被看到。** 分数、SP、名次、年份一律用衬线大字号承担视觉主角；标签、口径说明退到次要灰阶。用户扫一眼就该拿到结论。
4. **可被追问的排版。** 出处、口径、排序规则这些"证据"永远留在界面上（哪怕是小字），不藏在折叠层或 tooltip 里。
5. **一致性高于新奇。** 图标同源、间距成阶梯、颜色受 token 约束；新组件优先复用既有 token 与既有交互模式，而不是引入第二套视觉语言。
6. **键盘与对比度不是可选项。** 所有可交互元素有可见焦点环；正文对比度达 WCAG AA。这是课程作业的加分项，也是"庄重"的一部分。

## 第六轮补充（2026-09-23 · 新手指引与荷宝）

新增两个组件：**分步指引**（`.tour-layer` / `.tour-spot` / `.tour-card`）与**虚拟形象荷宝**
（`_mascot.html`，4 姿势内联 SVG）。逐条对照上面六条原则：

| 原则 | 这一轮的落地 |
|---|---|
| 1 实景要看得见 | 荷宝是新绘的三维感平涂小角色，不是照片；背景**完全透明**（无 `<rect>` 底色），放在卡片纸面上，不压任何插画 |
| 2 动静服务状态 | 只动 `opacity` 与 `transform`；`prefers-reduced-motion` 下不做浮动画与平滑滚动；`html.tour-on` 期间把 `scroll-behavior` 降为 `auto`，保证脚本量到的是落位后的坐标 |
| 3 数字与结论先被看到 | 指引卡每步一句结论式标题（「先挑一局题」），正文退到 `--ink-2` 小字；进度用「第 2 / 8 步」直说，不用圆点当唯一指示 |
| 4 可被追问的排版 | 指引卡底部常驻署名行（`.tour-credit`）：本项目自绘、原型为南开大学官微学生团队原创 IP「紫菡」 |
| 5 一致性高于新奇 | 复用既有 `.btn` / `.card` / `--radius-l` / `--shadow-lift` 与纸白 + 深墨 token，未引入第二套视觉语言；荷宝胸前纹样的银杏叶与品牌徽记同源 |
| 6 键盘与对比度 | `Tab` 锁在卡内循环、`Esc` 关闭、焦点交还；正文 6.4:1、小字 4.9:1，均在暗底之上达标；装饰图形 `aria-hidden` |

**明确不做的**：不用深色遮罩压白字盖图（高亮区用 `box-shadow` 挖洞 + 纸标签，与本项目既有做法一致）；
不用 emoji；不给荷宝加光泽渐变与发光描边。

**离线入口沿用同一套视觉**：`dist/` 里的单文件页面与安卓 APK 复用同一份 `app.css` 与同一批荷宝 SVG，
没有第二套样式。唯一的例外是 **APK 应用图标** —— 自适应图标的前景不适合透明（圆形 / 方形遮罩下会露出
系统底色，边缘发脏），所以图标里的荷宝铺在纸色 `rgb(250,245,234)` 底上、外描一圈赭色圆角边框。
**页面内的荷宝依旧是透明背景**，两处不冲突。

## 已知审计结论（2026-09-21 · 第一版审计）

> **状态：本节全部条目均已处理完毕，作为「踩过哪些坑」的历史留存。**
> 其中 #1 在**第五轮**被推翻重做 —— 不是「怎么把 8 格排好」，而是「这 8 格不该出现在知识库」
> （见 `docs/DESIGN.md` §5.7）。排查同类问题时，先问一句**这个板块该不该在这个页面上**，
> 再问怎么排版。

以下问题来自逐页截图复核 + CSS 根因定位，已按 P0/P1 归档：

| 级别 | 问题 | 根因 |
|---|---|---|
| P0 | ~~知识库数字看板 8 格排成 7+1，末尾格子悬空~~ **（第五轮整块删除）** | `.kb-stats` 用 `repeat(auto-fit, minmax(150px, 1fr))`，1188px 容器下解出 7 列 |
| P0 | 知识库 Hero 装饰圆环与「1919 建校」徽记相交，像错位 | `.kb-hero::after` 定位在右下角，半径 260px 覆盖了网格第二列的徽记 |
| P0 | 登录页/首页实景图呈灰绿浑浊 | `.hero-veil` / `.auth-veil` 近黑遮罩过重（最深处 0.94/0.96） |
| P0 | 侧栏 7 个导航图标字重、基线、视觉大小都不一致 | 用 ◆ ❖ ▤ ▲ ★ ▦ ◈ 七种 Unicode 符号拼凑 |
| P1 | 全站只有 `input:focus` 有焦点样式，键盘用户看不到焦点 | 缺 `:focus-visible` 规则 |
| P1 | 答题页在 1180px 高度下大片空白，且快捷键无人知晓 | 无快捷键实现、无提示卡 |
| P1 | 实景图过重拖慢首屏（libr 858KB / mainhall 234KB） | 未压缩；且 mainhall 仅 900×600 被当全屏背景拉伸 |
| P1 | 登录页独立于 base.html，缺 `html.anim` 与 favicon | 模板未继承 base，head 处理被漏掉 |
